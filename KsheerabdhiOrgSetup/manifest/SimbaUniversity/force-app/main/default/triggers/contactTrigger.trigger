trigger contactTrigger on Contact (before insert,after insert,before delete,after delete,before update,after update,after undelete) {
   /* if(trigger.isafter&&trigger.isinsert){
        ContactTriggerHandler.NumOfConForAcc(trigger.new);
    }
    if(trigger.isafter&&trigger.isdelete){
        ContactTriggerHandler.NumOfConForAcc(trigger.new);
    }
    if(trigger.isafter&&trigger.isupdate){
        ContactTriggerHandler.NumOfConForAcc(trigger.new);
    }*/
}