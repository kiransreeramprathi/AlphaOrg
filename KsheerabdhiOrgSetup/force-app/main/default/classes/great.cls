public class great 
{
  public static void number(integer a, integer b, integer c)
  {
      if ( a>=b && a >=c){
        system.debug(a + 'is the largest number');
  }
    else if (b >= a && b >= c){
        system.debug(b + 'is the largest number ');
    }
    else{
        system.debug(c +'is the largest number');
                     }
}
}