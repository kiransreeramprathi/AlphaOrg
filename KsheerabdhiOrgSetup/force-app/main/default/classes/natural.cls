public class natural 
{
    public static void number(integer n)
    {
        system.debug('eneter the value'+n);
        for(integer i=0;i<n;i++)
        {
        system.debug(i+1);
        }
    }
}